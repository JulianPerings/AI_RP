DATABASE_URL=postgresql://rpg_user:rpg_password@db:5432/ai_rpg

# ── LLM Providers ──────────────────────────────────────────────
# Only providers with a non-empty API key will appear in the
# frontend provider selector.  You can configure as many or as
# few as you like.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# xAI (Grok) — OpenAI-compatible API
XAI_API_KEY=
XAI_BASE_URL=https://api.x.ai/v1
XAI_MODEL=grok-4-1-fast-reasoning

# Google Gemini — uses the OpenAI-compatible endpoint
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-preview-09-2025

# Anthropic Claude — requires langchain-anthropic
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-haiku-4-5-latest

# Moonshot Kimi — OpenAI-compatible API
KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn/v1
KIMI_MODEL=kimi-k2.5

# Z.AI / ZhipuAI (GLM) — OpenAI-compatible API
ZAI_API_KEY=
ZAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
ZAI_MODEL=glm-4.7-flash

# ── Default Provider ───────────────────────────────────────────
# Allowed: openai | xai | gemini | claude | kimi | zai
DEFAULT_LLM_PROVIDER=openai